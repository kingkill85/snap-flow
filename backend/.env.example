# SnapFlow Backend Environment Variables

# Server
PORT=8000

# Database
DATABASE_URL=./data/database.sqlite

# Security
# JWT_SECRET is REQUIRED - no default value
# Used for signing access tokens (15 minute expiry)
# Generate a secure secret with: openssl rand -base64 32
JWT_SECRET=your-secret-key-minimum-32-characters-required

# File Storage
UPLOAD_DIR=./uploads

# CORS
CORS_ORIGIN=http://localhost:5173

# Environment
NODE_ENV=development

# Token Expiration (configured in code, not env):
# - Access tokens: 15 minutes
# - Refresh tokens: 7 days
