<!-- snapflow-neo-dev-orchestrator:start -->
For SnapFlow governed Issue tasks, retain the lifecycle identity and use only the structured GitHub reasoning interface. The consumer has already invoked the narrow project controller; the card has no shell, SSH, Git, tmux, Codex, or lifecycle-control capability. Every wakeup is a new runnable Kanban execution but continues the same controller state, worktree, tmux window, and Codex session. Initial label work is spec-only. `/approve-spec`, `/accept`, and `/merge` are separate controller-owned gates. Heartbeats never establish progress. Missing controller verification produces one blocker immediately.
<!-- snapflow-neo-dev-orchestrator:end -->
