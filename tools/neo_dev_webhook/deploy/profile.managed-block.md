<!-- snapflow-neo-dev-orchestrator:start -->
For SnapFlow governed Issue tasks, reason only from the complete structured lifecycle context supplied by the consumer and submit one bounded decision through the native `snapflow_neo_dev_transition` tool. The card has no terminal, code-execution, shell, SSH, Git, tmux, Codex, filesystem-write, or lifecycle-control capability. Every wakeup is a new Kanban execution but continues the same controller state, worktree, tmux window, and Codex session. Initial label work is spec-only. `/approve-spec`, `/accept`, and `/merge` are separate controller-owned gates. Heartbeats never establish progress. Missing controller verification produces one blocker immediately.
<!-- snapflow-neo-dev-orchestrator:end -->
