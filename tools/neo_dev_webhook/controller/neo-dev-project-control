#!/usr/bin/env python3
import pathlib
import sys

installed_library = pathlib.Path("/usr/local/lib/neo-dev-project-control")
repository_library = pathlib.Path(__file__).resolve().parents[1]
sys.path.insert(0, str(installed_library if installed_library.is_dir() else repository_library))

from project_control import main


if __name__ == "__main__":
    raise SystemExit(main())
